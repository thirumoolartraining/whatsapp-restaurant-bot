/**
 * Job Type Definitions
 * Phase 4.2: Async Infrastructure - Step 1 Foundation
 * 
 * Defines constants for job types that can be queued.
 * Minimal placeholder definitions for Step 1.
 */

const JOB_TYPES = {
  SEND_WHATSAPP_MESSAGE: 'SEND_WHATSAPP_MESSAGE',
  SEND_WHATSAPP_BROADCAST: 'SEND_WHATSAPP_BROADCAST'
};

module.exports = {
  JOB_TYPES
};
